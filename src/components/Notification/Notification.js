import React from 'react';
import { NotificationBlock } from '../../styles';

const Notification = ({ text }) => {
  return <NotificationBlock>{text}</NotificationBlock>;
};
export default Notification;
